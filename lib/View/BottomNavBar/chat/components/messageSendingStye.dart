import 'package:flutter/material.dart';

Widget sendingStyle() {
  return Column(
    children: [
      Container(
        padding: EdgeInsets.symmetric(horizontal: 20),
        height: 30,
        color: Colors.red,
      )
    ],
  );
}
